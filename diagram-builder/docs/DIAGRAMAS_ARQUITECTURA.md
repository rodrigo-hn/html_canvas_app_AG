# 🎨 Diagramas de Arquitectura - DiagramBuilder App

## 📋 Tabla de Contenidos
1. [Arquitectura General](#1-arquitectura-general)
2. [Flujo de Datos con Signals](#2-flujo-de-datos-con-signals)
3. [Ciclo de Vida de un Nodo](#3-ciclo-de-vida-de-un-nodo)
4. [Sistema de Drag & Drop](#4-sistema-de-drag--drop)
5. [Renderizado Polimórfico](#5-renderizado-polimórfico)
6. [Exportación HTML](#6-exportación-html)
7. [Jerarquía de Componentes](#7-jerarquía-de-componentes)
8. [Modelo de Datos](#8-modelo-de-datos)

---

## 1. Arquitectura General

```
┌─────────────────────────────────────────────────────────────────┐
│                        BOOTSTRAP LAYER                           │
│                                                                  │
│  ┌──────────┐         ┌────────────┐        ┌──────────────┐   │
│  │ main.ts  │────────>│ app.config │───────>│ App Component│   │
│  └──────────┘         │  (providers)│        │  (root)      │   │
│                       └────────────┘        └──────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                                │
                                │ RouterOutlet
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      PRESENTATION LAYER                          │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              CanvasComponent                              │  │
│  │  ┌─────────────────────────────────────────────────┐     │  │
│  │  │  @for (node of nodes(); track node.id)          │     │  │
│  │  │    ├─> NodeRendererComponent                    │     │  │
│  │  │    ├─> NodeRendererComponent                    │     │  │
│  │  │    └─> NodeRendererComponent                    │     │  │
│  │  └─────────────────────────────────────────────────┘     │  │
│  │                                                            │  │
│  │  [Export HTML Button] ───> HtmlExportService              │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                │
                                │ inject()
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                        SERVICE LAYER                             │
│                                                                  │
│  ┌────────────────────┐  ┌──────────────────┐  ┌─────────────┐ │
│  │ DiagramService     │  │ StencilService   │  │ HtmlExport  │ │
│  │                    │  │                  │  │ Service     │ │
│  │ • nodesSignal      │  │ • shapes registry│  │             │ │
│  │ • edgesSignal      │  │ • getSVG()       │  │ • export()  │ │
│  │ • selectionSignal  │  │                  │  │             │ │
│  │                    │  │                  │  │             │ │
│  │ providedIn: 'root' │  │ providedIn:root  │  │ providedIn  │ │
│  └────────────────────┘  └──────────────────┘  └─────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                                │
                                │ uses
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                        DATA LAYER                                │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                  diagram.model.ts                         │  │
│  │                                                            │  │
│  │  DiagramNode                                              │  │
│  │      ├─> ShapeNode (SVG)                                  │  │
│  │      └─> WebNode (Tailwind Components)                    │  │
│  │                                                            │  │
│  │  DiagramEdge                                              │  │
│  │  DiagramModel                                             │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. Flujo de Datos con Signals

### 2.1 Arquitectura de Signals (Estado Reactivo)

```
┌─────────────────────────────────────────────────────────────────────┐
│                      DiagramService (Singleton)                      │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │              PRIVATE STATE (Writable Signals)               │    │
│  │                                                              │    │
│  │  nodesSignal = signal<DiagramNode[]>([])                    │    │
│  │       │                                                      │    │
│  │       ├─ update((nodes) => [...nodes, newNode])             │    │
│  │       ├─ update((nodes) => nodes.filter(...))               │    │
│  │       └─ update((nodes) => nodes.map(...))                  │    │
│  │                                                              │    │
│  │  selectionSignal = signal<Set<string>>(new Set())           │    │
│  │       │                                                      │    │
│  │       └─ update((sel) => { ... return newSet })             │    │
│  └────────────────────────────────────────────────────────────┘    │
│                             │                                        │
│                             │ asReadonly()                          │
│                             ▼                                        │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │              PUBLIC API (Readonly Signals)                  │    │
│  │                                                              │    │
│  │  readonly nodes = this.nodesSignal.asReadonly()             │    │
│  │  readonly selection = this.selectionSignal.asReadonly()     │    │
│  │                                                              │    │
│  │  Components can READ but NOT WRITE                          │    │
│  └────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ inject(DiagramService)
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        COMPONENTS                                    │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────┐      │
│  │  CanvasComponent                                          │      │
│  │                                                            │      │
│  │  nodes = this.diagramService.nodes  ← ReadonlySignal     │      │
│  │                                                            │      │
│  │  Template:                                                 │      │
│  │  @for (node of nodes(); track node.id) {                  │      │
│  │    <app-node-renderer [node]="node" />                    │      │
│  │  }                                                         │      │
│  │                                                            │      │
│  │  ✓ Auto-updates when signal changes                       │      │
│  └──────────────────────────────────────────────────────────┘      │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────┐      │
│  │  NodeRendererComponent                                     │      │
│  │                                                            │      │
│  │  isSelected = computed(() =>                               │      │
│  │    this.diagramService.selection().has(this.node.id)      │      │
│  │  )                                                         │      │
│  │                                                            │      │
│  │  Template:                                                 │      │
│  │  [class.ring-2]="isSelected()"                            │      │
│  │                                                            │      │
│  │  ✓ Auto-recalculates when selection changes               │      │
│  └──────────────────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 Flujo de Actualización de Estado

```
USER ACTION: Click en un nodo
         │
         ▼
┌─────────────────────────────────────────┐
│  NodeRendererComponent.onSelect()       │
│  ────────────────────────────────────   │
│  onSelect(event: MouseEvent) {          │
│    const meta = event.metaKey;          │
│    this.diagramService                  │
│      .toggleSelection(id, meta);        │
│  }                                      │
└─────────────────────────────────────────┘
         │
         │ Call service method
         ▼
┌─────────────────────────────────────────┐
│  DiagramService.toggleSelection()       │
│  ────────────────────────────────────   │
│  toggleSelection(id, multi) {           │
│    this.selectionSignal.update(sel => { │
│      const newSel = new Set(sel);       │
│      newSel.add(id);                    │
│      return newSel;  ← NEW STATE        │
│    });                                  │
│  }                                      │
└─────────────────────────────────────────┘
         │
         │ Signal emits change
         ▼
┌──────────────────────────────────────────────────────────┐
│         SIGNAL PROPAGATION (Automatic)                   │
│                                                           │
│  selection signal changed                                │
│         │                                                 │
│         ├──────────────────┐                             │
│         │                  │                             │
│         ▼                  ▼                             │
│  Component A          Component B                        │
│  isSelected()         otherComputed()                    │
│  recalculates         recalculates                       │
│         │                  │                             │
│         └──────┬───────────┘                             │
│                │                                          │
│                ▼                                          │
│         TEMPLATES UPDATE                                 │
│         (Automatic DOM patch)                            │
└──────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  VISUAL UPDATE                           │
│  ────────────────────────────────────   │
│  <div [class.ring-2]="isSelected()">    │
│       ↑                                  │
│       └─ Class applied/removed           │
│                                          │
│  Blue ring appears on selected node     │
└─────────────────────────────────────────┘
```

---

## 3. Ciclo de Vida de un Nodo

### 3.1 Creación y Renderizado

```
PHASE 1: INITIALIZATION
═══════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────┐
│  DiagramService Constructor                               │
│  ──────────────────────────────────────────────────────  │
│  constructor() {                                         │
│    this.addNode({                                        │
│      id: '1',                                            │
│      type: 'shape',                                      │
│      shapeType: 'rectangle',                             │
│      x: 100, y: 100,                                     │
│      width: 120, height: 80,                             │
│      zIndex: 1,                                          │
│      data: { text: 'Start Process' }                     │
│    });                                                   │
│  }                                                       │
└──────────────────────────────────────────────────────────┘
         │
         │ addNode() called
         ▼
┌──────────────────────────────────────────────────────────┐
│  nodesSignal.update()                                     │
│  ──────────────────────────────────────────────────────  │
│  this.nodesSignal.update((nodes) =>                      │
│    [...nodes, newNode]  ← IMMUTABLE UPDATE               │
│  );                                                      │
│                                                           │
│  BEFORE: []                                              │
│  AFTER:  [{ id: '1', type: 'shape', ... }]               │
└──────────────────────────────────────────────────────────┘
         │
         │ Signal notifies subscribers
         ▼

PHASE 2: RENDERING
═══════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────┐
│  CanvasComponent Template                                 │
│  ──────────────────────────────────────────────────────  │
│  @for (node of nodes(); track node.id) {                 │
│    <app-node-renderer [node]="node" />                   │
│  }                                                       │
│                                                           │
│  ✓ Loop executes for each node                          │
│  ✓ track node.id optimizes re-renders                   │
└──────────────────────────────────────────────────────────┘
         │
         │ Creates component instance
         ▼
┌──────────────────────────────────────────────────────────┐
│  NodeRendererComponent Created                            │
│  ──────────────────────────────────────────────────────  │
│  @Input() node = { id: '1', type: 'shape', ... }         │
│                                                           │
│  isSelected = computed(() =>                             │
│    this.diagramService.selection().has('1')              │
│  )  ← Returns: false                                     │
└──────────────────────────────────────────────────────────┘
         │
         │ Conditional rendering based on node.type
         ▼

┌─────────────────────────────────────────────────────────────┐
│  IF node.type === 'shape'                                   │
│  ─────────────────────────────────────────────────────────  │
│  <svg viewBox="0 0 120 80">                                 │
│    <g [innerHTML]="getShapeContent()">                      │
│      ├─> StencilService.getShapeSVG('rectangle', 120, 80)  │
│      │                                                       │
│      └─> Returns: <rect width="120" height="80" ... />     │
│    </g>                                                     │
│                                                              │
│    <foreignObject width="120" height="80">                  │
│      <div>Start Process</div>  ← Text overlay               │
│    </foreignObject>                                         │
│  </svg>                                                     │
└─────────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│  FINAL DOM OUTPUT                                            │
│  ─────────────────────────────────────────────────────────  │
│  <div style="position: absolute; left: 100px; top: 100px;"  │
│       class="absolute select-none"                          │
│       appDraggable>                                         │
│    <svg>                                                     │
│      <rect width="120" height="80" fill="white" />          │
│      <div>Start Process</div>                               │
│    </svg>                                                   │
│  </div>                                                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. Sistema de Drag & Drop

### 4.1 Flujo Completo de Arrastre

```
PHASE 1: MOUSE DOWN
═══════════════════════════════════════════════════════════════

User clicks on node
         │
         ▼
┌──────────────────────────────────────────────────────────┐
│  DraggableDirective.onMouseDown()                         │
│  ──────────────────────────────────────────────────────  │
│  @HostListener('mousedown', ['$event'])                  │
│  onMouseDown(event: MouseEvent) {                        │
│    this.isDragging = true;                               │
│    this.initialMouse = {                                 │
│      x: event.clientX,   // e.g., 450                    │
│      y: event.clientY    // e.g., 320                    │
│    };                                                    │
│    this.initialPos = {                                   │
│      x: this.startPosition.x,  // e.g., 100              │
│      y: this.startPosition.y   // e.g., 100              │
│    };                                                    │
│    this.dragStart.emit();                                │
│    event.preventDefault();                               │
│  }                                                       │
└──────────────────────────────────────────────────────────┘

PHASE 2: MOUSE MOVE
═══════════════════════════════════════════════════════════════

User drags mouse
         │
         ▼
┌──────────────────────────────────────────────────────────┐
│  DraggableDirective.onMouseMove()                         │
│  ──────────────────────────────────────────────────────  │
│  @HostListener('document:mousemove', ['$event'])         │
│  onMouseMove(event: MouseEvent) {                        │
│    if (!this.isDragging) return;                         │
│                                                           │
│    // Calculate delta                                    │
│    const deltaX = event.clientX - this.initialMouse.x;   │
│    //            520 - 450 = 70                          │
│    const deltaY = event.clientY - this.initialMouse.y;   │
│    //            380 - 320 = 60                          │
│                                                           │
│    // Calculate new position                             │
│    let newX = this.initialPos.x + deltaX;                │
│    //        100 + 70 = 170                              │
│    let newY = this.initialPos.y + deltaY;                │
│    //        100 + 60 = 160                              │
│                                                           │
│    // SNAP TO GRID (gridSize = 20)                       │
│    if (this.snapToGrid) {                                │
│      newX = Math.round(newX / 20) * 20;                  │
│      //    Math.round(170/20)*20 = 9*20 = 180            │
│      newY = Math.round(newY / 20) * 20;                  │
│      //    Math.round(160/20)*20 = 8*20 = 160            │
│    }                                                     │
│                                                           │
│    this.dragMove.emit({ x: newX, y: newY });             │
│  }                                                       │
└──────────────────────────────────────────────────────────┘
         │
         │ Event emitted
         ▼
┌──────────────────────────────────────────────────────────┐
│  NodeRendererComponent.onDragMove()                       │
│  ──────────────────────────────────────────────────────  │
│  onDragMove(pos: Point) {                                │
│    this.diagramService.updateNode(                       │
│      this.node.id,                                       │
│      { x: pos.x, y: pos.y }  ← { x: 180, y: 160 }        │
│    );                                                    │
│  }                                                       │
└──────────────────────────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────┐
│  DiagramService.updateNode()                              │
│  ──────────────────────────────────────────────────────  │
│  updateNode(id: string, changes: Partial<DiagramNode>) { │
│    this.nodesSignal.update((nodes) =>                    │
│      nodes.map((n) =>                                    │
│        n.id === id                                       │
│          ? { ...n, ...changes }  ← MERGE                 │
│          : n                                             │
│      )                                                   │
│    );                                                    │
│  }                                                       │
│                                                           │
│  BEFORE: { id: '1', x: 100, y: 100, ... }                │
│  AFTER:  { id: '1', x: 180, y: 160, ... }                │
└──────────────────────────────────────────────────────────┘
         │
         │ Signal updates
         ▼
┌──────────────────────────────────────────────────────────┐
│  Template Re-renders                                      │
│  ──────────────────────────────────────────────────────  │
│  <div [style.left.px]="node.x"   ← 180                   │
│       [style.top.px]="node.y">   ← 160                   │
│                                                           │
│  ✓ DOM updated with new position                        │
│  ✓ Node moves smoothly on screen                        │
└──────────────────────────────────────────────────────────┘

PHASE 3: MOUSE UP
═══════════════════════════════════════════════════════════════

User releases mouse
         │
         ▼
┌──────────────────────────────────────────────────────────┐
│  DraggableDirective.onMouseUp()                           │
│  ──────────────────────────────────────────────────────  │
│  @HostListener('document:mouseup')                       │
│  onMouseUp() {                                           │
│    if (!this.isDragging) return;                         │
│    this.isDragging = false;                              │
│    this.dragEnd.emit({ x: newX, y: newY });              │
│  }                                                       │
└──────────────────────────────────────────────────────────┘
         │
         ▼
    DRAG COMPLETE
```

### 4.2 Snapping Algorithm Visualizado

```
Original Position: 170px

Grid Size: 20px

┌────┬────┬────┬────┬────┬────┬────┬────┬────┬────┬────┐
│ 0  │ 20 │ 40 │ 60 │ 80 │100 │120 │140 │160 │180 │200 │
└────┴────┴────┴────┴────┴────┴────┴────┴────┴────┴────┘
                                           ↑
                                         170px
                                      (user dragged)

Snapping Calculation:
───────────────────────────────────────────────────────────
Math.round(170 / 20) * 20
           ↓
Math.round(8.5) * 20
           ↓
         9 * 20
           ↓
         180px  ← SNAPPED POSITION

┌────┬────┬────┬────┬────┬────┬────┬────┬────┬────┬────┐
│ 0  │ 20 │ 40 │ 60 │ 80 │100 │120 │140 │160 │180 │200 │
└────┴────┴────┴────┴────┴────┴────┴────┴────┴────┴────┘
                                                ↑
                                              180px
                                         (final position)
```

---

## 5. Renderizado Polimórfico

### 5.1 Decisión de Renderizado

```
┌────────────────────────────────────────────────────────────────┐
│              NodeRendererComponent                              │
│                                                                 │
│  Input: node = {                                                │
│    id: '1',                                                     │
│    type: 'shape' | 'web-component',  ← DISCRIMINATOR            │
│    ...                                                          │
│  }                                                              │
└────────────────────────────────────────────────────────────────┘
                          │
                          │ Type check
                          ▼
            ┌─────────────────────────┐
            │   if (node.type === ?)  │
            └─────────────────────────┘
                     │         │
        ┌────────────┘         └────────────┐
        │                                   │
        ▼                                   ▼
┌──────────────────┐              ┌──────────────────┐
│  TYPE: 'shape'   │              │ TYPE: 'web-      │
│                  │              │  component'      │
└──────────────────┘              └──────────────────┘
        │                                   │
        ▼                                   ▼

╔═══════════════════════════════════════════════════════════════╗
║                     SVG RENDERING PATH                         ║
╚═══════════════════════════════════════════════════════════════╝

<svg viewBox="0 0 ${width} ${height}">
  <g [innerHTML]="getShapeContent()">
       │
       ├─> getShapeContent() {
       │     if (node.type !== 'shape') return '';
       │     const shapeNode = node as ShapeNode;
       │     return this.stencilService.getShapeSVG(
       │       shapeNode.shapeType,  ← 'rectangle', 'cylinder', etc.
       │       node.width,
       │       node.height
       │     );
       │   }
       │
       └─> StencilService.getShapeSVG()
                │
                ▼
       ┌──────────────────────────────────┐
       │  shapes: Record<string, Fn>      │
       │  {                                │
       │    'rectangle': (w,h) => {...},  │
       │    'cylinder': (w,h) => {...},   │
       │    'bpmn-task': (w,h) => {...}   │
       │  }                                │
       │                                  │
       │  generator = shapes[shapeType]   │
       │  return generator(width, height) │
       └──────────────────────────────────┘
                │
                ▼
       Returns SVG string:
       "<rect width='120' height='80' fill='white' stroke='black'/>"

  </g>

  <foreignObject>  ← Text overlay
    <div>{{ node.data.text }}</div>
  </foreignObject>
</svg>

╔═══════════════════════════════════════════════════════════════╗
║                  WEB COMPONENT RENDERING PATH                  ║
╚═══════════════════════════════════════════════════════════════╝

<div *ngIf="node.type === 'web-component'">
  <app-web-node-wrapper [node]="asWebNode(node)">
       │
       ├─> asWebNode(node: DiagramNode): WebNode {
       │     return node as WebNode;
       │   }
       │
       └─> WebNodeWrapperComponent
                │
                ▼
       ┌──────────────────────────────────┐
       │  @switch (node.componentType)    │
       │                                  │
       │  @case ('button') {              │
       │    <app-web-button               │
       │      [text]="node.data.text"     │
       │      [variant]="node.data.variant"│
       │    />                            │
       │  }                               │
       │                                  │
       │  @case ('card') {                │
       │    <app-web-card                 │
       │      [title]="node.data.title"   │
       │      [content]="node.data.content"│
       │    />                            │
       │  }                               │
       │                                  │
       │  @case ('input') {               │
       │    <app-web-input ... />         │
       │  }                               │
       └──────────────────────────────────┘
                │
                ▼
       Renders Tailwind Component:
       <button class="px-4 py-2 rounded bg-blue-500 hover:bg-blue-600">
         Save
       </button>
</div>
```

### 5.2 Ejemplo Completo: Dos Nodos Diferentes

```
INPUT DATA
══════════════════════════════════════════════════════════

nodes = [
  {
    id: '1',
    type: 'shape',
    shapeType: 'rectangle',
    x: 100, y: 100,
    width: 120, height: 80,
    data: { text: 'Start' }
  },
  {
    id: '2',
    type: 'web-component',
    componentType: 'button',
    x: 300, y: 100,
    width: 100, height: 40,
    data: { text: 'Save', variant: 'success' }
  }
]

RENDERING OUTPUT
══════════════════════════════════════════════════════════

┌────────────────────────────────────────────────────────┐
│                    Canvas                               │
│                                                         │
│  ┌─────────────────┐        ┌──────────────┐          │
│  │ SVG Rectangle   │        │  [  Save  ]  │          │
│  │                 │        │   (button)   │          │
│  │     Start       │        │              │          │
│  │                 │        └──────────────┘          │
│  └─────────────────┘          ↑                        │
│          ↑                    Web Component            │
│       Shape Node              (Tailwind CSS)           │
│       (SVG Path)                                       │
│                                                         │
└────────────────────────────────────────────────────────┘

DOM STRUCTURE
══════════════════════════════════════════════════════════

<div class="canvas">
  <!-- Node 1: Shape -->
  <div style="left: 100px; top: 100px;" appDraggable>
    <svg viewBox="0 0 120 80">
      <rect width="120" height="80" fill="white" stroke="black"/>
      <foreignObject>
        <div>Start</div>
      </foreignObject>
    </svg>
  </div>

  <!-- Node 2: Web Component -->
  <div style="left: 300px; top: 100px;" appDraggable>
    <button class="px-4 py-2 rounded bg-green-500 hover:bg-green-600">
      Save
    </button>
  </div>
</div>
```

---

## 6. Exportación HTML

### 6.1 Flujo de Exportación

```
USER CLICKS "Export HTML"
         │
         ▼
┌──────────────────────────────────────────────────────────┐
│  CanvasComponent.exportHtml()                             │
│  ──────────────────────────────────────────────────────  │
│  exportHtml() {                                          │
│    const html = this.htmlExportService.exportHtml({      │
│      nodes: this.diagramService.nodes(),  ← Get current  │
│      edges: []                                           │
│    });                                                   │
│                                                           │
│    // Download logic                                     │
│    const blob = new Blob([html], {type: 'text/html'});   │
│    const url = URL.createObjectURL(blob);                │
│    const a = document.createElement('a');                │
│    a.download = 'diagram-export.html';                   │
│    a.click();                                            │
│  }                                                       │
└──────────────────────────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────┐
│  HtmlExportService.exportHtml()                           │
│  ──────────────────────────────────────────────────────  │
│  exportHtml(model: DiagramModel): string {               │
│    const nodesHtml = model.nodes.map(node => {           │
│      if (node.type === 'shape') {                        │
│        return this.renderShape(node);                    │
│      } else {                                            │
│        return this.renderWebComponent(node);             │
│      }                                                   │
│    }).join('\n');                                        │
│                                                           │
│    return `<!DOCTYPE html>                               │
│      <html>                                              │
│        <head>                                            │
│          <script src="https://cdn.tailwindcss.com"/>     │
│        </head>                                           │
│        <body>                                            │
│          ${nodesHtml}                                    │
│        </body>                                           │
│      </html>`;                                           │
│  }                                                       │
└──────────────────────────────────────────────────────────┘
         │
         ├─────────────────┬─────────────────┐
         ▼                 ▼                 ▼
   renderShape()    renderWebComponent()  renderWebComponent()

┌────────────────────────────────────────────────────────────────┐
│  SHAPE RENDERING                                                │
│  ────────────────────────────────────────────────────────────  │
│  private renderShape(node: ShapeNode): string {                │
│                                                                 │
│    Step 1: Get SVG content                                     │
│    ─────────────────────────                                   │
│    const shapes = {                                            │
│      'rectangle': BasicShapes.rectangle,                       │
│      'cylinder': BasicShapes.cylinder,                         │
│      // ...                                                    │
│    };                                                          │
│    const generator = shapes[node.shapeType];                   │
│    const svgContent = generator(node.width, node.height);      │
│                                                                 │
│    Step 2: Wrap in positioned div                              │
│    ──────────────────────────────                              │
│    return `                                                    │
│      <div style="position: absolute;                           │
│                  left: ${node.x}px;                            │
│                  top: ${node.y}px;">                           │
│        <svg viewBox="0 0 ${node.width} ${node.height}">        │
│          ${svgContent}                                         │
│        </svg>                                                  │
│        ${node.data?.text ? `<div>${text}</div>` : ''}          │
│      </div>                                                    │
│    `;                                                          │
│  }                                                             │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│  WEB COMPONENT RENDERING                                        │
│  ────────────────────────────────────────────────────────────  │
│  private renderWebComponent(node: WebNode): string {           │
│    switch (node.componentType) {                               │
│      case 'button':                                            │
│        return this.renderButton(node);                         │
│      case 'card':                                              │
│        return this.renderCard(node);                           │
│    }                                                           │
│  }                                                             │
│                                                                 │
│  private renderButton(node: WebNode): string {                 │
│    const variants = {                                          │
│      primary: 'bg-blue-500 hover:bg-blue-600',                 │
│      success: 'bg-green-500 hover:bg-green-600',               │
│    };                                                          │
│    const cls = `px-4 py-2 ${variants[node.data.variant]}`;     │
│                                                                 │
│    return `                                                    │
│      <button style="position: absolute;                        │
│                     left: ${node.x}px;                         │
│                     top: ${node.y}px;"                         │
│              class="${cls}">                                   │
│        ${node.data.text}                                       │
│      </button>                                                 │
│    `;                                                          │
│  }                                                             │
└────────────────────────────────────────────────────────────────┘
         │
         │ All nodes rendered
         ▼
┌────────────────────────────────────────────────────────────────┐
│  FINAL HTML OUTPUT                                              │
│  ────────────────────────────────────────────────────────────  │
│  <!DOCTYPE html>                                               │
│  <html lang="en">                                              │
│  <head>                                                        │
│    <meta charset="UTF-8">                                      │
│    <script src="https://cdn.tailwindcss.com"></script>         │
│    <style>                                                     │
│      .diagram-container {                                      │
│        position: relative;                                     │
│        width: 100vw;                                           │
│        height: 100vh;                                          │
│      }                                                         │
│    </style>                                                    │
│  </head>                                                       │
│  <body>                                                        │
│    <div class="diagram-container">                             │
│                                                                 │
│      <!-- Shape Node -->                                       │
│      <div style="position: absolute; left: 100px; top: 100px;">│
│        <svg viewBox="0 0 120 80">                              │
│          <rect width="120" height="80" ... />                  │
│        </svg>                                                  │
│        <div>Start Process</div>                                │
│      </div>                                                    │
│                                                                 │
│      <!-- Button Node -->                                      │
│      <button style="position: absolute; left: 300px; ..."      │
│              class="px-4 py-2 bg-green-500 hover:bg-green-600">│
│        Save                                                    │
│      </button>                                                 │
│                                                                 │
│    </div>                                                      │
│  </body>                                                       │
│  </html>                                                       │
│                                                                 │
│  ✓ No Angular dependencies                                    │
│  ✓ Standalone HTML file                                       │
│  ✓ Tailwind CSS from CDN                                      │
│  ✓ Functional buttons with hover states                       │
└────────────────────────────────────────────────────────────────┘
```

---

## 7. Jerarquía de Componentes

```
┌────────────────────────────────────────────────────────────────┐
│                            App (Root)                           │
│                         app.ts (line 4)                         │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ <router-outlet></router-outlet>                           │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
                              │
                              │ Route: path: ''
                              ▼
┌────────────────────────────────────────────────────────────────┐
│                       CanvasComponent                           │
│                   canvas.component.ts (line 7)                  │
│                                                                 │
│  Properties:                                                    │
│  • nodes = this.diagramService.nodes  (ReadonlySignal)         │
│                                                                 │
│  Methods:                                                       │
│  • onBackgroundClick() → clearSelection()                      │
│  • exportHtml() → triggers download                            │
│                                                                 │
│  Template:                                                      │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ <div class="canvas">                                      │ │
│  │   <!-- Grid background -->                                 │ │
│  │   <div style="background: radial-gradient(...)"></div>    │ │
│  │                                                            │ │
│  │   <!-- Toolbar -->                                         │ │
│  │   <button (click)="exportHtml()">Export HTML</button>     │ │
│  │                                                            │ │
│  │   <!-- Nodes -->                                           │ │
│  │   @for (node of nodes(); track node.id) {                 │ │
│  │     <app-node-renderer [node]="node" />                   │ │
│  │   }                                                        │ │
│  │ </div>                                                     │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
                              │
                              │ Creates N instances
                              ▼
┌────────────────────────────────────────────────────────────────┐
│                    NodeRendererComponent                        │
│               node-renderer.component.ts (line 9)               │
│                                                                 │
│  Input:                                                         │
│  • @Input() node: DiagramNode  (required)                      │
│                                                                 │
│  Computed:                                                      │
│  • isSelected = computed(() => selection().has(node.id))       │
│                                                                 │
│  Methods:                                                       │
│  • onSelect(event) → toggleSelection()                         │
│  • onDragMove(pos) → updateNode()                              │
│  • getShapeContent() → StencilService.getShapeSVG()            │
│                                                                 │
│  Directives Applied:                                            │
│  • appDraggable                                                │
│                                                                 │
│  Template:                                                      │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ <div appDraggable                                         │ │
│  │      [class.ring-2]="isSelected()"                        │ │
│  │      [style.left.px]="node.x"                             │ │
│  │      (click)="onSelect($event)">                          │ │
│  │                                                            │ │
│  │   <!-- IF Shape -->                                        │ │
│  │   <svg *ngIf="node.type === 'shape'">                     │ │
│  │     <g [innerHTML]="getShapeContent()"></g>               │ │
│  │   </svg>                                                   │ │
│  │                                                            │ │
│  │   <!-- IF Web Component -->                                │ │
│  │   <div *ngIf="node.type === 'web-component'">             │ │
│  │     <app-web-node-wrapper [node]="asWebNode(node)" />     │ │
│  │   </div>                                                   │ │
│  │ </div>                                                     │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
                              │
                              │ When type === 'web-component'
                              ▼
┌────────────────────────────────────────────────────────────────┐
│                   WebNodeWrapperComponent                       │
│             web-node-wrapper.component.ts (line 8)              │
│                                                                 │
│  Input:                                                         │
│  • @Input() node: WebNode  (required)                          │
│                                                                 │
│  Template:                                                      │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ @switch (node.componentType) {                            │ │
│  │                                                            │ │
│  │   @case ('button') {                                      │ │
│  │     <app-web-button                                       │ │
│  │       [text]="node.data.text"                             │ │
│  │       [variant]="node.data.variant" />                    │ │
│  │   }                                                        │ │
│  │                                                            │ │
│  │   @case ('input') {                                       │ │
│  │     <app-web-input                                        │ │
│  │       [label]="node.data.label"                           │ │
│  │       [placeholder]="node.data.placeholder" />            │ │
│  │   }                                                        │ │
│  │                                                            │ │
│  │   @case ('card') {                                        │ │
│  │     <app-web-card                                         │ │
│  │       [title]="node.data.title"                           │ │
│  │       [content]="node.data.content" />                    │ │
│  │   }                                                        │ │
│  │ }                                                          │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
                              │
                              ├─────────────┬────────────┐
                              ▼             ▼            ▼
                    ┌──────────────┐ ┌───────────┐ ┌──────────┐
                    │ WebButton    │ │ WebInput  │ │ WebCard  │
                    │ Component    │ │ Component │ │ Component│
                    └──────────────┘ └───────────┘ └──────────┘
                    web-button       web-input     web-card
                    .component.ts    .component.ts .component.ts
```

---

## 8. Modelo de Datos

### 8.1 Jerarquía de Tipos

```
┌─────────────────────────────────────────────────────────────────┐
│                      TYPE HIERARCHY                              │
└─────────────────────────────────────────────────────────────────┘

                    ┌──────────────┐
                    │ DiagramModel │
                    └──────────────┘
                           │
                  ┌────────┴────────┐
                  ▼                 ▼
          ┌────────────┐    ┌──────────────┐
          │ nodes: []  │    │ edges: []    │
          └────────────┘    └──────────────┘
                  │                 │
                  ▼                 ▼
          DiagramNode[]      DiagramEdge[]


┌─────────────────────────────────────────────────────────────────┐
│                    DiagramNode (Interface)                       │
│  ─────────────────────────────────────────────────────────────  │
│  Extends: DiagramElement + Point + Size                         │
│                                                                  │
│  Properties:                                                     │
│  • id: string           (from DiagramElement)                   │
│  • zIndex: number       (from DiagramElement)                   │
│  • selected?: boolean   (from DiagramElement)                   │
│  • x: number            (from Point)                            │
│  • y: number            (from Point)                            │
│  • width: number        (from Size)                             │
│  • height: number       (from Size)                             │
│  • type: NodeType       ('shape' | 'web-component')             │
│  • data: any            (flexible data bag)                     │
│  • rotation?: number                                            │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ Specialized into
                  ┌───────────┴───────────┐
                  ▼                       ▼
┌──────────────────────────────┐  ┌──────────────────────────────┐
│   ShapeNode (Interface)      │  │   WebNode (Interface)        │
│  ──────────────────────────  │  │  ──────────────────────────  │
│  Extends: DiagramNode        │  │  Extends: DiagramNode        │
│                              │  │                              │
│  type: 'shape'  ← Literal    │  │  type: 'web-component'       │
│                              │  │                              │
│  shapeType: string           │  │  componentType: string       │
│    • 'rectangle'             │  │    • 'button'                │
│    • 'cylinder'              │  │    • 'input'                 │
│    • 'diamond'               │  │    • 'card'                  │
│    • 'bpmn-task'             │  │                              │
│    • 'bpmn-gateway'          │  │  data: {                     │
│    • ...                     │  │    text?: string,            │
│                              │  │    variant?: string,         │
│  style?: {                   │  │    title?: string,           │
│    fill?: string,            │  │    content?: string,         │
│    stroke?: string,          │  │    label?: string,           │
│    strokeWidth?: number      │  │    placeholder?: string,     │
│  }                           │  │    ...                       │
│                              │  │  }                           │
│  data: {                     │  │                              │
│    text?: string             │  │                              │
│  }                           │  │                              │
└──────────────────────────────┘  └──────────────────────────────┘
```

### 8.2 Ejemplo de Datos Reales

```typescript
// SHAPE NODE EXAMPLE
const shapeNode: ShapeNode = {
  // DiagramElement properties
  id: '1',
  zIndex: 1,
  selected: false,

  // Point properties
  x: 100,
  y: 100,

  // Size properties
  width: 120,
  height: 80,

  // DiagramNode properties
  type: 'shape',          // ← Type discriminator
  rotation: 0,

  // ShapeNode specific
  shapeType: 'rectangle', // ← Determines SVG renderer
  style: {
    fill: '#ffffff',
    stroke: '#000000',
    strokeWidth: 2
  },
  data: {
    text: 'Start Process'
  }
};

// WEB NODE EXAMPLE
const webNode: WebNode = {
  // DiagramElement properties
  id: '4',
  zIndex: 1,
  selected: false,

  // Point properties
  x: 400,
  y: 400,

  // Size properties
  width: 100,
  height: 40,

  // DiagramNode properties
  type: 'web-component',  // ← Type discriminator
  rotation: 0,

  // WebNode specific
  componentType: 'button', // ← Determines component renderer
  data: {
    text: 'Save',
    variant: 'success'
  }
};
```

### 8.3 Flujo de Transformación de Datos

```
SERVICE LAYER (State)
═══════════════════════════════════════════════════════════

DiagramService.nodesSignal = signal<DiagramNode[]>([
  { id: '1', type: 'shape', shapeType: 'rectangle', ... },
  { id: '2', type: 'web-component', componentType: 'button', ... }
])
         │
         │ asReadonly()
         ▼
readonly nodes: ReadonlySignal<DiagramNode[]>
         │
         │ inject(DiagramService)
         ▼

COMPONENT LAYER
═══════════════════════════════════════════════════════════

CanvasComponent:
  nodes = this.diagramService.nodes
         │
         │ Template: @for (node of nodes(); ...)
         ▼
  For each node → <app-node-renderer [node]="node" />
         │
         ▼

NodeRendererComponent:
  @Input() node: DiagramNode
         │
         │ Type check: node.type === ?
         ├────────────────┬────────────────┐
         ▼                ▼                ▼
   'shape'          'web-component'    unknown
         │                │
         ▼                ▼
   as ShapeNode      as WebNode
         │                │
         │                │
         ▼                ▼
   StencilService   WebNodeWrapper
   .getShapeSVG()         │
         │                │ @switch (node.componentType)
         │                ├─────────┬─────────┬─────────┐
         │                ▼         ▼         ▼         ▼
         │            'button'  'input'   'card'   'unknown'
         │                │         │         │
         │                ▼         ▼         ▼
         │          WebButton  WebInput  WebCard
         │          Component  Component Component
         │
         ▼
    SVG string → [innerHTML]="..."
         │
         ▼

RENDERED OUTPUT
═══════════════════════════════════════════════════════════

    DOM Elements (SVG or HTML with Tailwind)
```

---

## 📊 Resumen Visual

### Flujo Completo de la Aplicación

```
┌──────────────┐
│   USER       │
│  INTERACTION │
└──────┬───────┘
       │
       ├─── Click Node ─────────────────────┐
       │                                     ▼
       │                          ┌──────────────────┐
       │                          │ toggleSelection()│
       │                          │   (Service)      │
       │                          └────────┬─────────┘
       │                                   │
       ├─── Drag Node ──────────────┐     │ Signal Update
       │                             ▼     │
       │                   ┌──────────────┐│
       │                   │ updateNode() ││
       │                   │  (Service)   ││
       │                   └──────┬───────┘│
       │                          │        │
       ├─── Export HTML ────┐     │        │
       │                     ▼     ▼        ▼
       │              ┌──────────────────────────┐
       │              │   DiagramService         │
       │              │   ─────────────────      │
       │              │   • nodesSignal          │
       │              │   • selectionSignal      │
       │              └──────────┬───────────────┘
       │                         │
       │                         │ Signal Emits
       │                         ▼
       │              ┌──────────────────────────┐
       │              │   Component Layer        │
       │              │   ─────────────────      │
       │              │   • Canvas               │
       │              │   • NodeRenderer         │
       │              │   • Computed Signals     │
       │              └──────────┬───────────────┘
       │                         │
       │                         │ Template Update
       │                         ▼
       │              ┌──────────────────────────┐
       │              │   DOM Update             │
       │              │   ─────────────────      │
       │              │   • Position changed     │
       │              │   • Selection ring       │
       │              │   • Re-render nodes      │
       │              └──────────────────────────┘
       │
       └─────────────────────────────────────────┐
                                                  ▼
                                       ┌──────────────────┐
                                       │  Visual Feedback │
                                       │  ───────────────│
                                       │  User sees       │
                                       │  changes!        │
                                       └──────────────────┘
```

---

## 🎯 Puntos Clave

1. **Signals = Reactividad Automática**: Los cambios en signals propagan automáticamente a computed signals y templates
2. **Inmutabilidad**: Todas las actualizaciones crean nuevos objetos/arrays
3. **Type Safety**: TypeScript discriminated unions permiten renderizado polimórfico seguro
4. **Separación de Responsabilidades**: Service (estado) → Component (presentación) → Template (vista)
5. **Directivas Reutilizables**: `appDraggable` encapsula lógica de drag & drop
6. **Registry Pattern**: Mapeo de tipos a implementaciones (shapes, components)
7. **Exportación Desacoplada**: HTML generator independiente de Angular

